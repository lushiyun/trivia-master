import { Stack } from '@chakra-ui/core'

export const Main = (props) => (
  <Stack
    spacing="1.5rem"
    width="100%"
    maxWidth="48rem"
    minHeight="100vh"
    pt="8rem"
    px="1rem"
    {...props}
  />
)
